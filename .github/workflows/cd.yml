name: CD

# Deployment runs ONLY after the CI workflow completes successfully on main.
# Tag pushes (v*) additionally trigger the server deploy step.
on:
  workflow_run:
    workflows: ["CI"]
    types: [completed]
    branches: [main]
  push:
    tags: ["v*"]

jobs:
  build-and-push:
    name: Build & Push Docker Images
    runs-on: ubuntu-latest
    if: >-
      (github.event_name == 'workflow_run' && github.event.workflow_run.conclusion == 'success')
      || (github.event_name == 'push' && startsWith(github.ref, 'refs/tags/v'))
    # GITHUB_TOKEN needs packages: write to push images to GHCR; contents: read
    # for actions/checkout. No external secrets (DOCKER_USERNAME etc.) required.
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v7
        with:
          # For workflow_run events this checks out the commit that CI validated.
          ref: ${{ github.event.workflow_run.head_sha || github.sha }}

      - name: Resolve image tag
        id: meta
        run: |
          SHA="${{ github.event.workflow_run.head_sha || github.sha }}"
          echo "sha=${SHA}" >> "$GITHUB_OUTPUT"
          echo "sha_short=${SHA:0:12}" >> "$GITHUB_OUTPUT"
          # GHCR image names must be lowercase. github.repository is always
          # lowercase (owner/repo); github.actor / repository_owner are not.
          echo "owner=$(echo '${{ github.repository }}' | cut -d/ -f1)" >> "$GITHUB_OUTPUT"
          echo "registry=ghcr.io" >> "$GITHUB_OUTPUT"

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
          logout: true

      - name: Build & push backend
        uses: docker/build-push-action@v7
        with:
          context: ./backend
          file: ./backend/Dockerfile
          push: true
          tags: |
            ghcr.io/${{ steps.meta.outputs.owner }}/devsync-backend:latest
            ghcr.io/${{ steps.meta.outputs.owner }}/devsync-backend:${{ steps.meta.outputs.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Build & push frontend
        uses: docker/build-push-action@v7
        with:
          context: ./frontend
          file: ./frontend/Dockerfile
          push: true
          tags: |
            ghcr.io/${{ steps.meta.outputs.owner }}/devsync-frontend:latest
            ghcr.io/${{ steps.meta.outputs.owner }}/devsync-frontend:${{ steps.meta.outputs.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  # ── Deployment gate ──────────────────────────────────────────────
  # Deploy to production requires ALL of:
  #   1. CI passed (workflow_run success OR tag push)
  #   2. build-and-push succeeded
  #   3. Manual approval via GitHub Environments (configure a "production"
  #      environment with required reviewers in repo Settings → Environments)
  #
  # To deploy:
  #   1. Push a tag: git tag v1.0.0 && git push origin v1.0.0
  #   2. Wait for CD to build images
  #   3. Approve the "Deploy to Production" job in the GitHub Actions UI
  #
  # If the "production" environment does not exist or has no reviewers
  # configured, this job will be BLOCKED until approval is granted.
  deploy-server:
    name: Deploy to Production
    runs-on: ubuntu-latest
    needs: [build-and-push]
    if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/v')
    environment:
      name: production
      url: ${{ vars.DEPLOY_URL }}
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v7

      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.DEPLOY_HOST }}
          username: ${{ secrets.DEPLOY_USER }}
          key: ${{ secrets.DEPLOY_KEY }}
          script: |
            cd /opt/devsync
            docker compose pull
            docker compose up -d --remove-orphans
            docker image prune -f
