# Development: talk to the locally running Spring Boot backend.
# (These are the same values the code used to fall back to — now explicit.)
VITE_API_URL=http://localhost:8080/api
VITE_WS_URL=http://localhost:8080/ws
